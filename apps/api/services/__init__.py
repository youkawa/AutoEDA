"""Service layer for AutoEDA API (tools/evaluator/rag).

This keeps FastAPI routers thin and delegates logic to a dedicated layer,
in line with Clean Architecture boundaries (UI -> API -> Services).
"""

